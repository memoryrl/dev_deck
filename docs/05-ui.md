# 05. UI / Screen Design

시각 톤은 shadcn/ui 기본(중립 그레이 + 하나의 accent)으로 시작한다. 커스텀 브랜드 일러스트는 MVP에 넣지 않는다.

## 1. 라우트 맵

| 경로 | 그룹 | 인증 | 목적 |
| --- | --- | --- | --- |
| `/` | root | 공개 | 랜딩 / 프롬프트·커리어 티저 각 6개, 스킬 칩 |
| `/p/[id]` | root | 공개 | 공개 프롬프트 전문 |
| `/work` | root | 공개 | 커리어 게시판 (공개 글 전부) |
| `/work/[id]` | root | 공개 | 커리어 블로그 상세 |
| `/login` | `(auth)` | 비로그인 전용 | GitHub / Google |
| `/promptkit` | `(dashboard)` | 필수 | 프롬프트 목록 + 생성 |
| `/promptkit/[id]` | `(dashboard)` | 필수 | Owner 프롬프트 수정 |
| `/career` | `(dashboard)` | 필수 | 커리어 글 게시판 + 생성 |
| `/career/skills` | `(dashboard)` | 필수 | 스킬 인벤토리 |
| `/career/[id]` | `(dashboard)` | 필수 | Owner 커리어 글 수정 |
| `/steam` | `(dashboard)` | 필수 | 게임 라이브러리 |
| `/steam/[appid]` | `(dashboard)` | 필수 | 게임 + 리뷰 폼 |

미들웨어 가드: `/promptkit`, `/career`, `/steam` 및 하위. `/`, `/p`, `/work` 는 열어둠. `[id]`와 `skills`가 겹치지 않게 `skills` 정적 세그먼트를 동적보다 우선한다.

로그인 상태에서 `/login` 접근 시 `/promptkit`으로 보낸다.

## 2. 레이아웃

### 2.1 Root (`app/layout.tsx`)

- 폰트, `ThemeProvider` (**기본 `light`**, `dark` 클래스 토글), `Toaster`
- `next-themes` + 헤더 `ThemeToggle` (라이트 ↔ 다크). 시스템 테마를 기본값으로 쓰지 않는다.
- 선택 값은 localStorage에 유지
- 글로벌 네비는 랜딩·`/p`·`/work`에 심플 헤더 (홈, 커리어, 로그인, 테마)

### 2.2 Auth

- 중앙 카드. 로고 + 두 OAuth 버튼
- 마케팅 카피는 한 줄

### 2.3 Dashboard (`(dashboard)/layout.tsx`)

```text
┌──────────┬─────────────────────────────┐
│ Sidebar  │ Top bar (테마, 유저 아바타, Logout) │
│          ├─────────────────────────────┤
│ PromptKit│                             │
│ CareerLog│         children            │
│ Steam    │                             │
│          │                             │
└──────────┴─────────────────────────────┘
```

Sidebar 항목:

| 라벨 | href | 아이콘 (Lucide) |
| --- | --- | --- |
| PromptKit | `/promptkit` | `Terminal` 또는 `Sparkles` |
| CareerLog | `/career` | `Briefcase` |
| Steam Tracker | `/steam` | `Gamepad2` |

활성 라우트는 배경/폰트 weight로 구분. 모바일은 Sheet 사이드바.

## 3. 화면별 상태

모든 데이터 화면은 네 상태를 갖는다: **loading / empty / error / ready**.

### 3.1 Landing `/`

- 히어로: DevDeck, 한 줄 가치, CTA `로그인`, `스택 보기`(앵커)
- 모듈 카드 3장 → 로그인 또는 공개 앵커 (`/work`)
- 공개 프롬프트 최근 6개 → `/p/[id]`
- 공개 커리어 글 최근 6개 → `/work/[id]`. 더 보기 → `/work`
- 공개 스킬 칩 (`sort_order`). 없으면 해당 섹션 숨김
- 푸터: Next.js · Supabase · Steam API

### 3.1.1 공개 상세 `/p/[id]`

- 대시보드 셸 없음. 랜딩과 같은 공개 헤더
- 제목, 카테고리, 태그, Markdown 본문, 복사 버튼
- 수정/삭제/공개 토글 없음. Owner면 “대시보드에서 편집” 링크만
- 없거나 `is_public = false`이면 `notFound()`. 목록 6개 밖 공개 글도 URL이면 열림

### 3.1.2 공개 커리어 게시판 `/work`

- 공개 헤더. 제목: 커리어
- 필터: `post_type`, 회사, 스킬/태그 텍스트
- 카드: title, excerpt, type badge, company, period, tags
- 정렬: `created_at DESC`. 한도 없음
- Empty: “아직 공개된 글이 없습니다”

### 3.1.3 공개 커리어 상세 `/work/[id]`

- 블로그 레이아웃. 제목, type, 회사/역할/기간, 스킬·태그, Markdown 본문
- 수정 UI 없음. Owner면 “대시보드에서 편집”
- 없거나 비공개면 `notFound()`

### 3.2 Login `/login`

- GitHub, Google
- 에러 쿼리(`?error=`) 시 Alert

### 3.3 PromptKit 목록 `/promptkit`

**헤더:** 제목, `새 프롬프트` 버튼

**필터:** 텍스트 검색(제목/태그), 카테고리, 공개만

**카드:** title, category, tags, public badge, 복사 버튼, 상세 링크

**Empty:** “첫 프롬프트를 저장하세요” + 생성 CTA

**생성:** Dialog 또는 `/promptkit` 위 패널. 필드: title, category(텍스트 입력, placeholder `General`), tags(콤마), content(textarea), Markdown 미리보기 탭, is_public switch

### 3.4 PromptKit 상세 `/promptkit/[id]`

- 보기 / 편집 토글
- 복사, 공개 토글, 삭제(AlertDialog)
- 없으면 `notFound()`

### 3.5 CareerLog 목록 `/career`

**헤더:** 커리어, `새 글`, 스킬 관리(`/career/skills`)

**필터:** 검색, `post_type`, 회사, 공개만

**카드:** title, type, company, period, public badge

**Empty:** “참여했던 프로젝트를 글로 남겨 보세요”

**생성/수정 필드:** title, post_type(세그먼트 또는 select), excerpt, content+미리보기, company, role, period_start, period_end(비우면 진행 중), skills(콤마), tags(콤마), is_public

프로젝트 메타 필드는 `post_type = project`일 때 강조. 다른 타입이어도 입력은 허용한다.

### 3.6 CareerLog 상세 `/career/[id]`

- 보기 / 편집 토글
- 공개 토글, 삭제
- 없으면 `notFound()`

### 3.7 CareerLog 스킬 `/career/skills`

- 테이블 또는 카드 목록. `sort_order`로 위아래 (MVP는 숫자 입력으로도 충분)
- 필드: name, category, proficiency, years, summary, is_public
- 이름 중복 시 에러 토스트

### 3.8 Steam 목록 `/steam`

**헤더:** 게임 수, 총 플레이타임(분 → `Xh Ym`)

**정렬:** 플레이타임 desc (기본), 이름, 즐겨찾기 우선

**카드:** header.jpg, name, 플레이타임, 리뷰 있음 점, 즐겨찾기 별

**Empty:** Steam 설정 안내 (프로필 공개, env)

**Error:** 프록시 메시지 그대로 보여 주되 시크릿 없음

### 3.9 Steam 상세 `/steam/[appid]`

- 헤더 이미지, 이름, 플레이타임 (목록에서 못 찾으면 제목만 appid)
- 폼: rating, review_text, umpc_preset, is_favorite
- 저장 → toast → 폼 유지
- 삭제 리뷰

UMPC 프리셋은 자유 텍스트. MVP에서 프리셋 라이브러리 UI는 없다. placeholder 예시만 넣는다.

`ROG Ally / Lossless Scaling ON / 15W TDP / 1080p 60FPS`

## 4. 컴포넌트 인벤토리

### `components/ui` (shadcn)

Button, Input, Textarea, Label, Card, Badge, Dialog, AlertDialog, DropdownMenu, Sheet, Switch, Tabs, Separator, Avatar, Skeleton, Toast/Sonner, ScrollArea

### `components/layout`

- `AppSidebar`
- `UserMenu`
- `ThemeToggle`
- `PublicHeader`

### `components/promptkit`

- `PromptCard`
- `PromptForm`
- `PromptMarkdown` (sanitize)
- `CopyPromptButton`

### `components/career`

- `CareerPostCard`
- `CareerPostForm`
- `CareerSkillChip`
- `CareerSkillForm`
- `PeriodLabel` (`2021.03 – 2023.08` / `2024.01 – 현재`)

### `components/steam`

- `GameCard`
- `GameReviewForm`
- `PlaytimeLabel`

목록 페이지는 가능한 한 RSC. 복사·폼·OAuth만 Client Component.

## 5. 카피 톤

- 한국어 UI
- 짧은 문장. 대시보드 라벨은 명사 위주
- 에러는 원인과 다음 행동 한 줄 (`Steam 응답이 실패했습니다. 잠시 후 다시 열어 보세요.`)

## 6. 접근성 / 반응형

- 사이드바 접힘 < md
- 아이콘 버튼은 `sr-only` 라벨
- 복사 성공은 toast + 버튼 텍스트 일시 변경

## 7. 하지 않는 UI (MVP)

- 커스텀 차트 대시보드
- 드래그 앤 드롭 프롬프트 보드
- Steam 업적 그리드
- 커리어 댓글 / 커버 이미지 / 타임라인 연도 스크롤
- 다국어 i18n 프레임워크 (카피는 한국어 하드코드)
